---
description: An example slash command demonstrating the command format
argument-hint: <arg1> [optional-arg]
allowed-tools: [Read, Glob, Grep]
---

This is an example command. When invoked, perform the following:

1. Acknowledge the user's input arguments
2. Use the allowed tools to explore the codebase
3. Provide a helpful summary

Arguments:
- `arg1` (required): The primary input
- `optional-arg` (optional): Additional context
