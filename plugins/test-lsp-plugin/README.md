# Test LSP Plugin

Test plugin demonstrating LSP server integration for TypeScript/JavaScript.

## Configuration

The LSP server is configured in the marketplace manifest with:
- Command: `typescript-language-server --stdio`
- Supported extensions: `.ts`, `.tsx`, `.js`, `.jsx`
